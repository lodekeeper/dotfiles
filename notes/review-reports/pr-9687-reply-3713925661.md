Yes, that matches my read. Since `bestValidUpdates` can keep the previous sync periods across a store upgrade, normalizing the cached updates during `upgradeLightClientStore()` is the right place to prevent mixed pre-Gloas/Gloas header shapes from leaking into later summaries.
