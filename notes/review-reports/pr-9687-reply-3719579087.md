Confirmed on current head. `24ae721c` changes `bootstrap` to load as raw bytes, resolves `bootstrapFork` from `meta.bootstrap_fork_digest`, deserializes with `sszTypesFor(bootstrapFork).LightClientBootstrap`, and only then upgrades to `storeFork` when needed. Thanks, this addresses my comment.
