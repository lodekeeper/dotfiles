@gemini-code-assist This is handled on current head: `computeBranchRoot()` now checks `branch.length < depth` before slicing and entering the loop, so `proof[i]` is defined for the full expected branch depth. The check was added in `575528a7f44228eaf1c3e308717bfda8f125125b`.
