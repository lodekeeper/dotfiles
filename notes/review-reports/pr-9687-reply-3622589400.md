@gemini-code-assist I agree with the follow-up here that this one is not a real issue. `blockToLightClientHeader()` computes `beacon.bodyRoot` with the fork-specific `BeaconBlockBody.hashTreeRoot(block.body)` before reading `signedExecutionPayloadBid`, so a Gloas body missing that required field should already fail while hashing the body. Adding optional chaining would just move the failure and weaken the typed block-body assumption.
