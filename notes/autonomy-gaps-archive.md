# Autonomy Gaps — Archived "Next Audit Priorities" History

Archived 2026-09-06 from the live `## Next Audit Priorities (next daily cycles)`
section of `notes/autonomy-gaps.md`, item 1 (`Add claude-cli/* fallback to 4
Codex-only crons + fix cron-health-watchdog delivery`). That section had grown
to ~78KB / ~31K tokens for a single tracked item — big enough that a plain
`Read` of just the section exceeds the harness's 25K-token single-call limit,
plausibly why the 2026-09-05 `next-audit-priorities-reminder` cycle burned
502s/173,590 tokens and left no durable trace (see the 2026-09-06 entry in the
live file for the full writeup).

These are the full day-by-day re-check entries for that item, 2026-08-09
through 2026-09-02, preserved verbatim in original order. The live file keeps
only the most recent entries going forward; consult this file for older
history instead of re-deriving it.

---

   - **2026-08-09 03:1x UTC re-check:** live `cron.list` shows 2 of 4 self-healed on their own retry (Codex quota cleared, no config change applied yet): `cron-health-watchdog` (`consecutiveErrors: 0`, ok since 08-08 ~09:45 UTC) and `check-openclaw-release` (`consecutiveErrors: 0`, last 2 runs ok, most recent 08-08 21:58:46 UTC — recovered sometime after the 09:47 UTC check, not yet logged until now). The other two are still sitting at their pre-existing error counts and haven't had a scheduled retry since the 08-08 audit: `self-improvement-audit-daily` (`consecutiveErrors: 5`, next attempt 2026-08-09 03:16:29 UTC) and `nightly-memory-consolidation` (`consecutiveErrors: 7`, next attempt 2026-08-09 03:30:00 UTC) — both due within the next ~20 minutes of this check. This is consistent with the quota-exhaustion theory (self-heals on retry once quota resets) but does **not** address the underlying gap — no fallback config exists yet, so a future quota exhaustion window would silently break these same four crons again. No reply seen yet from Nico in the DM thread (inconclusive check, not re-escalating). Not re-DMing — no new decision-relevant fact, same ask as 08-08.
   - **2026-08-09 05:3x UTC re-check (second same-cycle nudge), new finding — `nightly-memory-consolidation` failures are NOT all quota-related:** `self-improvement-audit-daily` also self-healed (`consecutiveErrors: 0`, ran ok 03:16:29 UTC — 3 of 4 now recovered). `nightly-memory-consolidation` alone remains at `consecutiveErrors: 8` (up from 7) after its 03:30:00 UTC retry, but the failure signature changed from the prior two nights: `errorReason: timeout` (900857ms, matching this job's `timeoutSeconds: 900`), not `rate_limit`. Read the actual script log (`memory/memory-cycle-2026-08-09.log`) rather than trusting the cron-layer verdict: the underlying `nightly_memory_cycle.sh` run **completed successfully end-to-end at 03:58:57Z** — confirmed independently via fresh output artifacts, not just the log's own claim: `bank/state.json` (3.46MB, mtime 03:30), entity pages under `bank/entities/*/`(mtime 03:30), `.memory/index.sqlite` (mtime 03:30), and the QMD `daily-notes`/`memory-bank` collections (`qmd status` → "Updated: 2h ago", consistent with a run finishing ~03:58 checked at 05:3x). The cron harness's 900s timeout fires and marks the job `error` while the detached script keeps running past that window and finishes anyway.
     Pulled the last 10 `cron.runs` entries (08-04 through 08-09): only 2 of 10 (08-07, 08-08) were genuine `rate_limit`/quota failures. The other 8 were `timeout` — either ~15min (hitting the 900s ceiling on the primary nightly run) or ~1min (same-day retries failing faster) — and spot-checking the 08-05/08-06 script logs shows those **also** completed successfully ("Nightly memory cycle complete" logged, ~27-29min embedding step each night) despite being marked `error`. Root cause of the timeout class: chronic no-GPU/CUDA on this box (`nvcc` not found — present in every log back to at least 08-03, not a new regression) forces QMD's Step 4 embedding update onto a CPU-only fallback that alone takes 27-29 minutes, pushing total pipeline runtime well past the 900s/15min cron timeout regardless of which model backs Step 1.
     **Correction to the existing diagnosis:** the "4 crons silently broke from Codex quota exhaustion" framing only fully explains 2 of this cron's ~8 recent failure entries; the rest are a separate, unrelated, chronic timeout misconfiguration that's been mislabeling successful runs as errors for at least a week. A `claude-cli/*` fallback alone would **not** fix the timeout class — `timeoutSeconds` needs raising (e.g. to ~2700s/45min) to match real CPU-bound embedding runtime. Both fixes are cron-config edits needing Nico's sign-off (same precedent as the guard-stripper fix) — not applied unilaterally. Posted findings to topic #347; not re-DMing (refines an already-DMed item, doesn't introduce new urgency beyond what's already pending).
   - **2026-08-10 03:16 UTC daily audit improvement:** implemented a non-config watchdog-side mitigation while waiting for Nico's config sign-off. `scripts/cron/check_cron_health.py` now annotates future failing `nightly-memory-consolidation` timeout alerts with local completion/artifact evidence and the CPU-bound QMD embedding duration, so the watchdog can distinguish "cron harness timed out but pipeline completed" from a real incomplete memory-cycle run. Verified against the real 2026-08-09 log. Remaining config ask is unchanged: add cross-provider fallbacks where appropriate, raise nightly-memory timeout to about 2700s, and give `cron-health-watchdog` explicit topic #347 delivery.
   - **2026-08-10 09:31 UTC re-check (`next-audit-priorities-reminder`):** live `cron.list` shows 3 of 4 still healthy (`self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release` all `consecutiveErrors: 0`). `nightly-memory-consolidation` alone continues failing nightly on the same already-diagnosed timeout-while-actually-completing pattern (`consecutiveErrors: 9`, up from 8 — expected, not a new failure mode). Checked the Nico DM thread directly (`agent:main:telegram:direct:5774760693` session + transcript file): both still show last activity 2026-08-07 17:25 UTC, so no reply to the 08-08 sign-off ask has arrived. No new decision-relevant fact, so no re-DM and no repeat topic #347 post this cycle. Read-only verification; no config or script changes.
   - **2026-08-11 09:30 UTC re-check (`next-audit-priorities-reminder`):** live `cron.list` still shows 3 of 4 healthy (`self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release` all `consecutiveErrors: 0`). `nightly-memory-consolidation` ticked to `consecutiveErrors: 10` (up from 9) on the same already-diagnosed pattern — re-verified directly against today's log rather than trusting the cron-layer verdict: `memory/memory-cycle-2026-08-11.log` shows the pipeline actually completed (`Nightly memory cycle complete` at 03:56:03Z), ~26min run dominated by the chronic CPU-only QMD embedding step, well past the cron's 900s/15min timeout (fires ~03:45 UTC) but not an incomplete run. Checked the Nico DM thread transcript directly (`75297ea1-...jsonl`): its last activity is now 2026-08-10 19:06 UTC (later than the previously-noted 08-07 17:25 UTC), but that exchange (18:28-19:06 UTC) was entirely about the unrelated github-notifications deadlock/gateway-restart thread — grepped for fallback/sign-off/consolidation/timeoutSeconds/quota keywords and found nothing addressing this ask. Still no reply to the 08-08 sign-off request. No new decision-relevant fact, so no re-DM and no repeat topic #347 post this cycle. Read-only verification; no config or script changes.
   - **2026-08-12 09:33 UTC re-check (`next-audit-priorities-reminder`):** live `cron.list` still shows 3 of 4 target crons healthy (`self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release` all `consecutiveErrors: 0`). `nightly-memory-consolidation` ticked to `consecutiveErrors: 11` on the same already-diagnosed timeout-while-actually-completing pattern (`lastErrorReason: timeout`, `timeoutSeconds: 900`). Re-verified against today's local artifacts without editing memory files: `memory/memory-cycle-2026-08-12.log` ends with `Nightly memory cycle complete` at 03:56:06Z, QMD embedded 1227 chunks in 25m45s after the known CUDA/nvcc fallback, and `qmd status` reports collections updated 6h ago. No config changes applied; remaining ask is unchanged: raise nightly-memory timeout to about 2700s and add cross-provider fallbacks / explicit watchdog delivery after Nico signs off.
   - **2026-08-13 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron.list`/`cron get` still shows 3 of 4 target crons healthy (`self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release` all `consecutiveErrors: 0`). `nightly-memory-consolidation` ticked to `consecutiveErrors: 12` on the same already-diagnosed timeout-while-actually-completing pattern (`lastErrorReason: timeout`, `timeoutSeconds: 900`, `lastDurationMs: 900494`). Re-verified against today's local artifacts: `memory/memory-cycle-2026-08-13.log` ends with `Nightly memory cycle complete` at 03:57:52Z, QMD embedded 1268 chunks in 27m32s after the known CUDA/nvcc CPU fallback; `bank/state.json` and `.memory/index.sqlite` both fresh at 03:30 UTC; `qmd status` confirms collections updated ~6h ago. Also checked the Nico DM thread transcript directly (session `75297ea1-...`, superseded mid-check by a fresh session `efe90087-...` under the same DM key — both read): found genuinely new activity since the last check (2026-08-11 11:53 UTC casual check-in, then 2026-08-13 08:52-09:29 UTC), but none of it addresses this ask — it was a status query about an unrelated live Discord session (Lodestar shutdown-hang validation, already tracked in `BACKLOG.md`) and a separate, already-resolved `libp2p-pr3597` test-session wind-down. No reply to the 08-08 sign-off request. No new decision-relevant fact, so no re-DM and no repeat topic #347 post beyond routine status. Read-only verification; no config or script changes.
   - **2026-08-14 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron.list`/`cron get` still shows 3 of 4 target crons healthy (`self-improvement-audit-daily` `consecutiveErrors: 0`, last run 03:19 UTC ok; `cron-health-watchdog` `consecutiveErrors: 0`, last run 09:06 UTC ok; `check-openclaw-release` `consecutiveErrors: 0`, last run 2026-08-13 22:12 UTC ok). `nightly-memory-consolidation` ticked to `consecutiveErrors: 13` (up from 12) on the same already-diagnosed timeout-while-actually-completing pattern (`lastErrorReason: timeout`, `timeoutSeconds: 900`, `lastDurationMs: 900451`, run started 03:32:21 UTC). Re-verified against today's local artifacts rather than trusting the cron-layer verdict: `memory/memory-cycle-2026-08-14.log` ends with `Nightly memory cycle complete` at 03:57:58Z (QMD embedded 1201 chunks in 25m16s after the known CUDA/nvcc CPU fallback); `bank/state.json` (3.5MB) and `.memory/index.sqlite` (7.5MB) both fresh at 03:32:35 UTC. Checked the Nico DM thread transcript directly (session `efe90087-...`, `updatedAt` now 2026-08-13 22:33:45 UTC — newer than the last check): genuinely new activity (22:21-22:33 UTC), but unrelated to this ask — Nico flagged the `lodekeeper.github.io` blog appearing stale, the session found + fixed a real independent bug (two malformed `daily-journal` posts, Day 176 `$NOW_TS` literal + Day 115 control chars, had frozen GitHub Pages builds since 2026-07-27), pushed a fix (`0dc2a91`), and confirmed the backlog of posts rendered; closed out with Nico's ack. Grepped the full session for fallback/sign-off/consolidation/timeoutSeconds/quota keywords — zero hits. Still no reply to the 08-08 sign-off request. No new decision-relevant fact for *this* item, so no re-DM and no repeat topic #347 post beyond routine status. Read-only verification; no config or script changes.
   - **2026-08-15 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron get` still shows 3 of 4 target crons healthy (`self-improvement-audit-daily` `consecutiveErrors: 0` last run ok; `cron-health-watchdog` `consecutiveErrors: 0` last run ok; `check-openclaw-release` `consecutiveErrors: 0` last run ok). `nightly-memory-consolidation` ticked to `consecutiveErrors: 14` (up from 13) on the same already-diagnosed timeout-while-actually-completing pattern (`lastErrorReason: timeout`, `timeoutSeconds: 900`, `lastDurationMs: 901959`). Re-verified against today's local artifacts rather than trusting the cron-layer verdict: `memory/memory-cycle-2026-08-15.log` ends with `Nightly memory cycle complete` at 04:00:24Z (QMD embedded 1299 chunks in 30m3s after the known CUDA/nvcc CPU fallback — the longest embedding run in this series so far, consistent with a growing corpus, not a new failure mode); `bank/state.json` (3.5MB) and `.memory/index.sqlite` (7.5MB) both fresh at 03:30 UTC; `qmd status` confirms collections updated ~6h ago (851 files, 106718 vectors). Checked the Nico DM thread transcript directly (session `efe90087-...`): `updatedAt` unchanged at 2026-08-13 22:33:45 UTC — zero new activity since yesterday's check, so still no reply to the 08-08 sign-off request. No new decision-relevant fact, so no re-DM and no repeat topic #347 post beyond routine status. Read-only verification; no config or script changes.
   - **2026-08-16 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` shows all 4 target crons at `consecutiveErrors: 0` for the first time in this series — `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release` stayed healthy as before, and `nightly-memory-consolidation` flipped from `consecutiveErrors: 14` to `0` overnight (`lastRunStatus: ok`, `lastDurationMs: 743060`≈12m23s, run started 03:30:00 UTC). **Not the underlying fix landing** — the live payload still shows `timeoutSeconds: 900` unchanged, and no cron config was edited (state-object `updatedAtMs` timestamps line up exactly with each job's own last-run-completion time, not a manual edit). Verified against `memory/memory-cycle-2026-08-16.log`: last night's QMD embedding step processed only 547 chunks from 9 documents in 11m36s, versus the 1201-1299 chunks/25-30min seen every night 08-11 through 08-15 — a much lighter delta, most likely a knock-on effect of yesterday's unrelated `STATE.md` consolidation (BACKLOG.md 2026-08-16 08:10 UTC entry moved 161 old sweep-log entries to `STATE_ARCHIVE.md`, cutting corpus churn for QMD to re-embed), not a change to the chronic CPU-only-embedding bottleneck (`nvcc`/CUDA still unavailable, confirmed again in tonight's log). The 900s timeout is still one heavy memory-writing night away from tripping again — treat tonight's all-green `cron.list` as coincidental, not resolved, in any later read. Checked the Nico DM thread transcript directly (session `efe90087-...`, `updatedAt` now 2026-08-15T09:34:10Z — a short exchange shortly after yesterday's check ran, so yesterday's "zero new activity" note is superseded): grepped the full transcript for fallback/sign-off/consolidation/timeoutSeconds/quota/2700 keywords and confirmed this exchange was entirely the separate, already-resolved `gh-notif-cron-self-verify` efficiency proposal (ended with Nico: "leaving the cron as-is, no change") — nothing addressing this ask. Still no reply to the 08-08 sign-off request, now 8 days pending. No new decision-relevant fact for *this* item, so no re-DM and no repeat topic #347 post beyond routine status (calibrated-restraint precedent — not re-escalating on elapsed time alone). Read-only verification; no config or script changes.
   - **2026-08-17 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` again shows all 4 target crons at `consecutiveErrors: 0`, but today's clean snapshot is a backoff-retry artifact, not a rerun of 08-16's genuine light night. Pulled `cron.runs` history directly for `nightly-memory-consolidation` (job `4aaaf7f7-...`) instead of trusting the single latest-state snapshot: the regular 03:30:00 UTC run hit the same already-diagnosed harness timeout (`status: error`, `errorReason: timeout`, `durationMs: 900451`) — cross-checked against `memory/memory-cycle-2026-08-17.log`, which shows the underlying pipeline actually completed at 03:56:17Z (1145 chunks / 10 documents, 25m57s CPU-bound QMD embedding on the chronic nvcc/CUDA-unavailable fallback, unchanged root cause). A second, automatic retry then fired at 04:45:44 UTC and returned `ok` in 39.5s — not because the timeout was fixed, but because Step 1 and Step 4 found nothing new to consolidate or embed (`Added: 0`, `Skipped duplicates: 64`, `✓ All content hashes already have embeddings.`) since the 03:30 run had already finished the real work 49 minutes earlier. Net: this is the identical 08-11-through-08-15 failure mode, just self-healed by the retry-finds-nothing-new path before this cycle's check landed, rather than still showing red. The other 3 target crons remain genuinely healthy (`self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release` all `consecutiveErrors: 0`, most recent runs 08-16/08-17 `ok`). Checked the Nico DM thread directly (session `efe90087-...`): `updatedAt` still 2026-08-15T09:34:10Z, byte-identical to yesterday's check — zero new activity, still no reply to the 08-08 sign-off request, now 9 days pending. No new decision-relevant fact, so no re-DM and no repeat topic #347 post beyond routine status (calibrated-restraint precedent — not re-escalating on elapsed time alone). Read-only verification; no config or script changes.
   - **2026-08-18 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` shows the same config gap still present: all four target jobs still have no `fallbacks`, `cron-health-watchdog` still has `delivery.mode: none`, and `nightly-memory-consolidation` still has `timeoutSeconds: 900`. Current state snapshot is green-ish (`self-improvement-audit-daily`, `check-openclaw-release`, and `nightly-memory-consolidation` at `consecutiveErrors: 0`; `cron-health-watchdog` last run ok and currently running), but `cron.runs` again confirms the memory job's regular 03:30 UTC attempt timed out first (`status: error`, `errorReason: timeout`, `durationMs: 931490`) before an automatic retry returned ok at 03:57:39 UTC in 49.5s. Cross-check against `memory/memory-cycle-2026-08-18.log`: the underlying first run completed at 03:57:29 UTC after CPU-only QMD embedding of 1067 chunks from 14 documents in 26m36s; the retry found `Added: 0`, `Skipped duplicates: 50`, and `✓ All content hashes already have embeddings.` and completed at 03:58:11 UTC. `bank/state.json`, `.memory/index.sqlite`, and `qmd status` are fresh. Net: no new failure mode, but the original config blocker is still live — the latest `ok` status is retry-after-completion masking, not resolution. No config or memory-file edits.
   - **2026-08-19 09:4x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` shows the config gap still present for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, and `nightly-memory-consolidation` still have no `fallbacks`; `cron-health-watchdog` still has `delivery.mode: none`; `nightly-memory-consolidation` still has `timeoutSeconds: 900`. Current state snapshot is superficially green for the four (`consecutiveErrors: 0`), but `cron.runs --id 4aaaf7f7-...` again shows the regular 03:30 memory run timed out at 03:45 (`durationMs: 900521`) before an automatic retry completed at 03:52 (`durationMs: 289723`). New operational finding this cycle: the retry overlapped the first still-running memory cycle and both wrote the same QMD index/log; `memory/memory-cycle-2026-08-19.log` has two Step 4 sections, a first completion at 03:50 after 841 chunks / 19m41s CPU embedding, then duplicate-vector warnings and `RangeError: Invalid count value: -6` before the second completion at 03:51. `qmd status` still reports fresh indexes (`863` files, `110328` vectors), but the existing watchdog returned `NO_REPLY` because `scripts/cron/check_cron_health.py` was reading a nonexistent local jobs snapshot (`/home/openclaw/.openclaw/cron/jobs.json`) instead of the live gateway state. Non-config mitigation applied: `check_cron_health.py` now falls back to live `openclaw cron list --json` and adds a virtual `nightly-memory-qmd-index-health` alert for completed memory-cycle logs containing QMD index anomalies, so future watchdog runs no longer hide this overlap/corruption-risk shape. Verified with `python3 -m py_compile scripts/cron/check_cron_health.py` and a live run that reports `daily-journal` plus the QMD anomaly. Original config ask remains unchanged and still gated on Nico sign-off: add provider fallbacks where appropriate, raise nightly-memory timeout to about 2700s, and give `cron-health-watchdog` explicit topic #347 delivery.
   - **2026-08-20 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` shows the config gap still present for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, and `nightly-memory-consolidation` still have no `fallbacks`; `cron-health-watchdog` still has `delivery.mode: none`; `nightly-memory-consolidation` still has `timeoutSeconds: 900`. Current state snapshot shows all four at `consecutiveErrors: 0`, but `cron.runs --id 4aaaf7f7-...` again confirms this is retry-after-completion masking, not resolution: the regular 03:30:00 UTC run timed out at the cron-harness level (`status: error`, `errorReason: timeout`, `durationMs: 900527`, ending 03:45:00 UTC), and an automatic retry starting 03:46:53 UTC completed `ok` in 58.4s (03:47:51 UTC) — cross-checked against `memory/memory-cycle-2026-08-20.log` and fresh `bank/state.json` / `.memory/index.sqlite` artifacts (mtime ~03:47:09-10 UTC), consistent with the retry, not the killed run, having done the real work. Same chronic CPU-only QMD embedding bottleneck (`nvcc`/CUDA unavailable) driving the harness timeout; the retry's own run summary also logged a recurring QMD `memory-bank` indexing error (`entities/people/-.md has no valid filename content`), matching the `handelize`-style filename issue first noted 2026-08-19 — not a new failure mode, no action taken (below this cron's remit and not the config ask being tracked). The other 3 target crons remain genuinely healthy (`self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release` all `consecutiveErrors: 0`, most recent runs ok). Checked the Nico DM thread directly: the previously-tracked session (`efe90087-...`) has been superseded by a new session (`agent:main:telegram:direct:5774760693`, id `1426909a-...`, last activity 2026-08-19 22:57 UTC); grepped its full transcript for fallback/sign-off/consolidation/timeoutSeconds/quota/2700 keywords — the only hits are incidental (a pasted daily-note dump mentioning routine memory-consolidation cron runs, and pasted historical 403-rate-limit memory content); the actual live conversation that session covers is entirely GitHub external-contributor push-permission denials (#9848, lodestar-z#559, #9855, #9858), unrelated to this ask. Still no reply to the 08-08 sign-off request, now 12 days pending. No new decision-relevant fact, so no re-DM and no repeat topic #347 post beyond routine status (calibrated-restraint precedent — not re-escalating on elapsed time alone). Read-only verification; no config or memory-file edits.
   - **2026-08-21 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` shows the config gap still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, and `nightly-memory-consolidation` still have no `fallbacks`; `cron-health-watchdog` still has `delivery.mode: none`; `nightly-memory-consolidation` still has `timeoutSeconds: 900`. State snapshot shows all four at `consecutiveErrors: 0`, but `cron.runs --id 4aaaf7f7-...` again confirms retry-after-completion masking, not resolution: the regular 03:30:00 UTC run timed out at the cron-harness level (`status: error`, `errorReason: timeout`, `durationMs: 900445`, ending 03:45:00 UTC), and an automatic retry starting 03:45:44.856 UTC completed `ok` in 32.4s. Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` advanced to 2026-08-20 23:23:45 UTC (new activity since yesterday's 22:57 UTC mark), grepped the fetched history for fallback/sign-off/consolidation/timeoutSeconds/quota/2700/nightly-memory/cron-health-watchdog keywords — zero hits; the actual conversation was an unrelated Discord meme-posting task. Still no reply to the 08-08 sign-off request, now **13 days pending**. No new decision-relevant fact for the config ask, so no re-DM and no repeat topic #347 post beyond routine status (calibrated-restraint precedent).
     **Separate blocker actually advanced this cycle — root-caused and fixed the recurring QMD `memory-bank` crash (not the config ask, but a real gap I could resolve unilaterally without Nico sign-off):** the retry's auto-generated summary today was explicit for the first time that the crash isn't cosmetic — `"QMD memory-bank update hit an error on invalid generated path entities/people/-.md, so that collection refresh was incomplete despite the script exiting 0"` — meaning 3 nights running (08-19, 08-20, 08-21) the memory-bank collection's BM25/FTS index silently went stale after that point in the file walk. Traced root cause in `scripts/memory/generate_entity_pages.py`: `MENTION_RE = re.compile(r"@([A-Za-z0-9_-]+)")` allows a bare/leading-hyphen capture, so any memory text that literally quotes `@-`-style examples (e.g. `` `gh api -f body=@-` `` shell syntax, or prose like "no `@-mention`") gets mis-parsed as a person mention named `-`/`-ask`/`-mention`/`-mentions`, and `write()` persists it as `bank/entities/people/-.md` etc. QMD's `handelize()` then throws on the bare-hyphen filename during the nightly `[2/3] memory-bank` indexing pass, aborting that collection's refresh (the shell script's `|| true` guard hides the crash from the cron's exit code, which is why `cron.list` kept reporting `ok`). **Fix applied:** tightened the regex to `r"@([A-Za-z0-9][A-Za-z0-9_-]*)"` (require a leading alphanumeric) — verified in isolation against 6 real usernames from this workspace's own mentions (`nflaig`, `lodekeeper`, `GrapeBaBa`, `chatgpt-codex-connector`, `spiral-ladder`) plus an email address (`nflaig@protonmail.com` → `protonmail`, an unrelated pre-existing false positive, left alone — out of scope for this fix) — all legit captures unaffected, all 4 bare-hyphen variants (`@-`, `@--`, `@-ask`, `@-mention(s)`) now produce zero matches. `python3 -m py_compile` clean. Deleted the 4 stale garbage files this bug had already produced (`bank/entities/people/{-,-ask,-mention,-mentions}.md` — confirmed by inspection to be 100% self-referential noise quoting the bug pattern itself, not real people; `bank/entities/` is untracked/fully machine-regenerated from `bank/state.json` every night, so nothing of value was lost) since the generator never prunes stale outputs and they'd have re-crashed tonight's indexer even with the code fix alone. **Verified end-to-end with a live `qmd update`** (indexing only, not the slow `qmd embed` step): all 3 collections — `daily-notes`, `memory-bank`, `workspace-core` — now complete cleanly, `memory-bank` reporting `9 new, 3 updated, 311 unchanged, 2 removed` with **no handelize error**, confirming tonight's 03:30 UTC nightly-memory-consolidation run should no longer hit this crash. **Known non-blocking residual (flagging, not fixing today — genuine scope creep beyond this crash fix):** `MENTION_RE` still over-matches a broader noise class unrelated to the crash — npm scopes (`@sigstore`, `@typescript-eslint`, `@libp2p`, `@chainsafe`), version strings (`@v4`, `@v5`), email domains (`@protonmail`/`@fastmail`), bare numeric IDs, and literal uses of the word "@mention" in prose — evidenced by `bank/entities/people/{0,1,...,7,100992,314771702418505728,300s,v0,v4,v5,mention,mentions,chainsafe,libp2p,sigstore,typescript,users,fastmail,protonmail}.md` still on disk. None of these crash anything (only the bare-hyphen class did), so left untouched; a real fix needs a deliberate allow/deny-list or heuristic design decision, not a one-line regex patch — worth a dedicated look, not a rushed addition to a daily audit cron. No `memory/*.md` files edited (hard constraint honored); no cron config changed (still gated on Nico's sign-off above).
   - **2026-08-22 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` shows the config gap still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, and `nightly-memory-consolidation` still have no `fallbacks`; `cron-health-watchdog` still has `delivery.mode: none`; `nightly-memory-consolidation` still has `timeoutSeconds: 900`. State snapshot shows all four at `consecutiveErrors: 0`, but `cron.runs --id 4aaaf7f7-...` again confirms retry-after-completion masking, not resolution: the regular 03:30:01 UTC run timed out at the cron-harness level (`status: error`, `errorReason: timeout`, `durationMs: 901989`, ending ~03:45 UTC), and an automatic retry starting 03:47:30 UTC completed `ok` in 883484ms (~14m43s).
     **Severity step-change from prior nights: tonight's overlap caused real (not cosmetic) partial embedding data loss.** The retry's own run summary explicitly states it "overlapped with an already-running 03:30 UTC cycle" — same overlap shape first seen 08-19/08-20, but this time the collision hit mid-embed instead of mid-log-write: `memory/memory-cycle-2026-08-22.log` shows the run's QMD embedding step failed 269 of 1261 chunks with `SessionReleasedError: LLM session has been released or aborted` (the overlapping second run's own embed pass, invoked while the first was still holding the model session, got its session yanked out from under it mid-chunk), including **this cron's own artifacts** — `2026-08-22.md` chunk 0 (today's daily note), all 4 chunks of `2026-08-21.md`, `identity.md` chunks 0-3, `soul.md` chunks 2-5, plus several entity pages and eth-rnd-archive notes. Verified this is a real, scoped gap rather than a repeat of 08-19's cosmetic `RangeError`: `qmd get daily-notes/2026-08-22.md` and `qmd ls daily-notes` confirm the *document* layer (Step 3 SQLite FTS, what `qmd search`/`qmd get` read) is present and current — the gap is isolated to the *vector embedding* layer (Step 4, what `qmd vsearch`/`qmd query` read) for exactly those 269 chunks. Expected to self-heal on the next nightly run that doesn't overlap (embed only processes content hashes still missing vectors, same as the routine "✓ All content hashes already have embeddings" pattern seen on every prior clean night) — not a permanent loss, but the first confirmed case of this recurring overlap actually degrading data quality rather than just tripping the cron-harness's status field. This is fresh supporting evidence for the same already-pending ask (raising `timeoutSeconds` would give the first run room to finish before any retry fires, eliminating the overlap window entirely), not a new decision. Also confirmed the 08-21 bare-hyphen `MENTION_RE` fix is holding: grepped tonight's log for `handelize`/`has no valid filename` — zero hits, no regression.
     Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` is byte-identical to yesterday's check (2026-08-20T23:23:45Z) — zero new activity since yesterday, confirmed by reading the thread's last 15 messages (entirely an unrelated Discord meme-repost task). Still no reply to the 08-08 sign-off request, now **14 days pending**. No new decision-relevant fact for the config ask itself, so no re-DM and no repeat topic #347 post beyond routine status (calibrated-restraint precedent, re-verified against today's live facts rather than pattern-matched on elapsed time alone). No config or memory-file edits.
   - **2026-08-23 09:31 UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` shows the config gap still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, and `nightly-memory-consolidation` still have no `fallbacks`; `cron-health-watchdog` still has `delivery.mode: none`; `nightly-memory-consolidation` still has `timeoutSeconds: 900`. State snapshot shows all four at `consecutiveErrors: 0` — and unlike most recent nights, tonight's `nightly-memory-consolidation` run (03:31:04-03:44:44 UTC, `lastDurationMs: 819999`≈13m40s) is a genuine clean single run, not retry-after-completion masking: it finished under the 900s timeout with no overlap, cross-checked against `memory/memory-cycle-2026-08-23.log` (484 chunks / 8 documents, 13m5s CPU-bound QMD embedding on the still-unavailable nvcc/CUDA fallback — a much lighter night, second occurrence of this pattern after 08-16, most likely corpus-size variance rather than the fix landing). New minor diagnostic noted (not a blocker): the cron state now carries a `lastDiagnostics` warning — `` `ps -o pid,ppid,stat,etime,cmd --forest -p 90430 --ppid 90430 (agent)` failed `` — from the `check_cron_health.py` process-tree probe added 08-19; harmless (target process had already exited by probe time), no action needed. Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` still byte-identical to 2026-08-20T23:23:45Z — zero new activity for the third straight day (last content is the unrelated Discord meme-repost task). Still no reply to the 08-08 sign-off request, now **15 days pending**. No new decision-relevant fact, so no re-DM and no repeat topic #347 post beyond routine status (calibrated-restraint precedent). Read-only verification; no config or memory-file edits.
   - **2026-08-25 09:3x UTC re-check (`next-audit-priorities-reminder`) — explains the missing 08-24 entry, new failure signature root-caused, no data loss:** live `cron list --json` shows the config gap still present and unchanged for the original four jobs: no `fallbacks` on any of them, `cron-health-watchdog` still `delivery.mode: none`, `nightly-memory-consolidation` still `timeoutSeconds: 900`. There is no 2026-08-24 entry in this section because **this audit cron itself didn't run that morning** — it was one of ~11 crons caught in a gateway-wide outage. Root-caused via `cron runs --id 4aaaf7f7-...` (nightly-memory-consolidation) plus the Nico DM thread: starting at the 03:30:00 UTC scheduled run, `nightly-memory-consolidation` hit a **new failure signature** never seen before in this saga — `cron: isolated agent setup timed out before runner start`, ~60-62s each, 4x back-to-back (03:31:59, 03:34:11, 03:37:34, 03:43:37 UTC) — categorically different from the chronic `job execution timed out (last phase: tool-execution-started)` ~900s class. Unlike every prior "timeout" night, **`memory/memory-cycle-2026-08-24.log` was never created** — the script never got far enough to start, not a harness mislabel of a completed run. This is the first genuine full-night miss in the 17-day saga. Cross-checked against the Nico DM thread: an automatic post-restart health check (session `agent:main:telegram:direct:5774760693`, 10:34-10:56 UTC) shows the gateway process itself was only 425s old at 10:34 UTC (restarted ~10:27 UTC) and confirms Nico restarted the gateway to fix a broken "spawn path"; the same check's fleet snapshot lists `next-audit-priorities-reminder` and `nightly-memory-consolidation` both at `consecutiveErrors: 4` alongside 9 other jobs, all flagged as stale-error/backoff expected to self-clear on next scheduled run — exactly matching what `cron.runs` shows directly. Root cause of *why* the gateway needed restarting isn't visible from here (no journal/sudo access to `openclaw-gateway.service` from this account — confirmed via `systemctl show`/`journalctl`, both empty for this user); this was Nico's own fix, already applied, nothing to escalate. **No data loss:** `scripts/memory/consolidate_from_daily.py` scans a rolling 7-day window (`--limit 7`) of `memory/*.md` with dedup, so `2026-08-24.md` (exists, 9.3KB, written 23:00 UTC that day) was swept up cleanly by tonight's run alongside the rest — no gap-filling needed. Tonight's (08-25) own `nightly-memory-consolidation` run reverted to the original already-diagnosed chronic class (`errorReason: timeout`, `durationMs: 901181`≈15min, tool-execution-started) — confirmed via `memory/memory-cycle-2026-08-25.log` (finished 03:58:03Z, 484 chunks/8 docs embedded) and fresh `bank/state.json`/`.memory/index.sqlite` (mtime 03:30) that the pipeline actually completed as usual; `consecutiveErrors: 5` is self-consistent (0 after 08-23's clean run, +4 from the 08-24 outage, +1 from tonight's ordinary timeout). Checked the DM thread for a reply to the 08-08 sign-off ask: grepped available history for fallback/sign-off/consolidation/timeoutSeconds/quota/2700/nightly-memory/cron-health-watchdog keywords — zero hits addressing it; the only relevant content was the unrelated post-restart verification above. Still no reply, now **17 days pending**. No re-DM (calibrated-restraint precedent — Nico already independently found and fixed the acute gateway issue same-day; nothing new for him to decide on the standing config ask). Posted routine status to topic #347. No config or memory-file edits; read-only investigation.
     **Fleet-wide sanity check (whole cron list, not just the 4 tracked jobs):** only 3 jobs still show `consecutiveErrors >= 3`: `next-audit-priorities-reminder` (4, but that's this cron's own pre-run state — expected to clear once this run finishes), `nightly-memory-consolidation` (5, already covered above), and `weekly-stale-review-findings` (4, same `isolated agent setup timed out before runner start` signature from the 08-24 10:20 UTC tail of the outage). Flagging `weekly-stale-review-findings` specifically: it's a Monday-10:00-UTC cron (`0 10 * * 1`), next scheduled run **2026-08-31** — it'll sit showing a stale error badge for 6 more days purely because it hasn't had a chance to retry yet, not because anything is still broken. Noting this so a future audit/health-check pass doesn't mistake a 6-day-old already-resolved outage artifact for a new live problem. No action taken (out of scope for this cron, will self-clear on its own schedule).
   - **2026-08-26 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` (read via job JSON, not the flattened summary view) confirms the config gap is still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, `nightly-memory-consolidation` all still have no `fallbacks` field on their payload; `cron-health-watchdog` still `delivery.mode: none`; `nightly-memory-consolidation` still `payload.timeoutSeconds: 900`. State: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release` all `consecutiveErrors: 0`, last runs ok. `nightly-memory-consolidation` at `consecutiveErrors: 6` (`lastErrorReason: timeout`, `lastDurationMs: 901444`≈15m01s) — same already-diagnosed harness-timeout-while-actually-completing pattern, not a new failure mode: `memory/memory-cycle-2026-08-26.log` shows the pipeline finished at 03:58:29Z (1174 chunks/8 documents, 27m49s CPU-bound QMD embedding on the still-unavailable nvcc/CUDA fallback), and `bank/state.json`/`.memory/index.sqlite` are fresh at 03:30 UTC. Grepped tonight's log for `handelize`/`has no valid filename`/`SessionReleasedError`/`RangeError` — zero hits, confirming the 08-21 `MENTION_RE` fix is still holding with no regression; `qmd status` shows all 3 collections indexed clean (`daily-notes` 476 files, `memory-bank` 302 files, `workspace-core` 86 files, all updated within the last 12h). Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` advanced to 2026-08-25T12:02:18Z (new activity since the 08-25 check), but it's an unrelated PR #6/#7 topic:50 coordination handoff for Marko — grepped the fetched history for fallback/sign-off/consolidation/timeoutSeconds/quota/2700/nightly-memory/cron-health-watchdog keywords and found zero substantive hits. Still no reply to the 08-08 sign-off request, now **18 days pending**. No new decision-relevant fact, so no re-DM and no repeat topic #347 post beyond routine status (calibrated-restraint precedent). Read-only verification; no config or memory-file edits.
   - **2026-08-27 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` confirms the config gap is still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, `nightly-memory-consolidation` all still have no `fallbacks` field on their payload; `cron-health-watchdog` still `delivery.mode: none`; `nightly-memory-consolidation` still `payload.timeoutSeconds: 900`. State: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release` all `consecutiveErrors: 0`, last runs ok. `nightly-memory-consolidation` at `consecutiveErrors: 7` (`lastErrorReason: timeout`, `lastDurationMs: 943420`≈15m43s, run 03:30:00-03:45:43 UTC) — same already-diagnosed harness-timeout-while-actually-completing pattern, not a new failure mode (no masking retry fired this time, same shape as 08-25/08-26): `memory/memory-cycle-2026-08-27.log` shows the pipeline actually finished at 04:00:47Z (1206 chunks/15 documents, 30m16s CPU-bound QMD embedding on the still-unavailable nvcc/CUDA fallback — the longest embedding run in this series yet, consistent with continued corpus growth, not a new problem), and `bank/state.json` (3.66MB)/`.memory/index.sqlite` (7.52MB) both fresh at 03:30 UTC; `qmd status` confirms all 3 collections updated ~6h ago (`daily-notes` 478 files, `memory-bank` 303 files, `workspace-core` 86 files). Grepped tonight's log for `handelize`/`has no valid filename`/`SessionReleasedError`/`RangeError`/`overlapped` — zero hits, confirming the 08-21 `MENTION_RE` fix is still holding with no regression. Fleet-wide sanity check (all 22 jobs, not just the 4 tracked): only 2 jobs at `consecutiveErrors >= 3` — this one (7) and `weekly-stale-review-findings` (unchanged at 4, next scheduled retry 2026-08-31T10:00 UTC per its Monday cadence, exactly as flagged 08-25 — still just an unretried outage artifact, not a new problem). Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` byte-identical to yesterday's check (2026-08-25T12:02:18Z) — zero new activity for the second straight day, still no reply to the 08-08 sign-off request, now **19 days pending**. No new decision-relevant fact, so no re-DM and no repeat topic #347 post beyond routine status (calibrated-restraint precedent). Read-only verification; no config or memory-file edits.
   - **2026-08-28 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` confirms the config gap is still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, `nightly-memory-consolidation` all still have no `fallbacks` field on their payload; `cron-health-watchdog` still top-level `delivery.mode: none`; `nightly-memory-consolidation` still `payload.timeoutSeconds: 900`. State: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release` all `consecutiveErrors: 0`, last runs ok. `nightly-memory-consolidation` at `consecutiveErrors: 8` (up from 7; `lastErrorReason: timeout`, `lastDurationMs: 901114`≈15m01s, run started 03:30:00 UTC) — same already-diagnosed harness-timeout-while-actually-completing pattern, not a new failure mode: `memory/memory-cycle-2026-08-28.log` shows the pipeline actually finished at 03:55:47Z (1116 chunks/10 documents, 25m26s CPU-bound QMD embedding on the still-unavailable nvcc/CUDA fallback), and `qmd status` confirms all 3 collections fresh (~6h ago; 870 files/118425 vectors total, `daily-notes` 480 files, `memory-bank` 304 files). Grepped tonight's log for `handelize`/`has no valid filename`/`SessionReleasedError`/`RangeError`/`overlapped` — zero hits (confirmed via explicit `$?`, not just absence of printed matches), confirming the 08-21 `MENTION_RE` fix is still holding with no regression. Fleet-wide sanity check: only 2 jobs at `consecutiveErrors >= 3` — this one (8) and `weekly-stale-review-findings` (unchanged at 4, last run still the 08-24 outage artifact, next scheduled retry 2026-08-31T10:00 UTC per its Monday cadence — unchanged from 08-25/26/27, not a new problem). Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` byte-identical to the last three checks (2026-08-25T12:02:18Z) — zero new activity for the fourth straight day (last content still the unrelated PR #6/#7 topic:50 coordination handoff for Marko), still no reply to the 08-08 sign-off request, now **20 days pending**. No new decision-relevant fact, so no re-DM and no repeat topic #347 post beyond routine status (calibrated-restraint precedent). Read-only verification; no config or memory-file edits.
   - **2026-08-29 09:34 UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` confirms the config gap is still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, `nightly-memory-consolidation` all still have no `fallbacks` field on their payload; `cron-health-watchdog` still top-level `delivery.mode: none`; `nightly-memory-consolidation` still `payload.timeoutSeconds: 900`. State: all four `consecutiveErrors: 0`, last runs ok. Tonight's `nightly-memory-consolidation` run (03:30:13-03:40:33 UTC, `lastDurationMs: 654220`≈10m54s) is a genuine clean single run, not retry-after-completion masking — finished well under the 900s timeout with no overlap, cross-checked against `memory/memory-cycle-2026-08-29.log` (431 chunks/7 documents, ~10min CPU-bound QMD embedding on the still-unavailable nvcc/CUDA fallback — a light night, third occurrence of this lighter-corpus pattern after 08-16 and 08-23, most likely corpus-size variance rather than the fix landing). `bank/state.json` (3.67MB) and `.memory/index.sqlite` (7.52MB) both fresh at 03:30 UTC; `qmd status` confirms all 3 collections updated ~6h ago (872 files/118856 vectors). Grepped tonight's log for `handelize`/`has no valid filename`/`SessionReleasedError`/`RangeError`/`overlapped` — zero hits (exit code 1, confirmed), confirming the 08-21 `MENTION_RE` fix is still holding with no regression. Fleet-wide sanity check: only 1 job at `consecutiveErrors >= 3` — `weekly-stale-review-findings` (unchanged at 4, still the 08-24 outage artifact, next scheduled retry 2026-08-31T10:00 UTC per its Monday cadence — unchanged from 08-25 through 08-28, not a new problem). Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` byte-identical to the last five checks (2026-08-25T12:02:18Z) — zero new activity for the fifth straight day, still no reply to the 08-08 sign-off request, now **21 days pending**. No new decision-relevant fact, so no re-DM and no repeat topic #347 post beyond routine status (calibrated-restraint precedent). Read-only verification; no config or memory-file edits.
   - **2026-08-30 09:3x UTC re-check (`next-audit-priorities-reminder`) — second confirmed overlap-induced embedding data loss (after 08-22), victim role reversed:** live `cron list --json` confirms the config gap is still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, `nightly-memory-consolidation` all still have no `fallbacks` field on their payload; `cron-health-watchdog` still `delivery.mode: none`; `nightly-memory-consolidation` still `payload.timeoutSeconds: 900`. State snapshot shows all four at `consecutiveErrors: 0` — but per `cron runs --id 4aaaf7f7-...` this is retry-after-completion masking again, not resolution: the regular 03:30:02.563 UTC run timed out at the cron-harness level (`status: error`, `errorReason: timeout`, `durationMs: 903730`≈15m04s, harness gave up 03:45:06 UTC), and an automatic retry starting 03:46:39.133 UTC completed `ok` in 738006ms≈12m18s (03:58:57 UTC).
     Cross-checked `memory/memory-cycle-2026-08-30.log` directly rather than trusting the cron-layer verdict, and it contains two full interleaved pipeline runs — disambiguated by matching each Step-4 completion line against the retry's own auto-generated summary text ("embeddings updated for 7 documents / 175 chunks"): the retry's Step 4 completed cleanly (`✓ Done! Embedded 175 chunks from 7 documents in 9m 42s`, "Nightly memory cycle complete" at 03:57:08Z) — zero failures. The other section in the same log — the original run, started 03:30:16Z, which the harness gave up waiting on at 03:45:06 UTC but which kept running detached (the long-established pattern) — finished its own full pipeline at **04:00:51Z** (~30m35s wall time) and is the one that hit real failures: `✓ Done! Embedded 1024 chunks from 11 documents in 30m 27s` / **`⚠ 184 chunks failed`**, all `SessionReleasedError: LLM session has been released or aborted`.
     This is the second confirmed instance (after 08-22's 269/1261) of the known overlap bug causing genuine partial embedding data loss, not just a cosmetic status mismatch — but with the victim role reversed: 08-22 concluded the *retry* (second-starting run) got its session yanked mid-embed when the first run finished; tonight it's the *original* run (first-starting, still mid-Step-4 when the retry launched at 03:46:39) whose chunks failed, most likely because the retry's own Step 4 grabbed and then released the shared embedding session around 03:56-58Z while the original run's Step 4 was still using it. Net: whichever of the two overlapping invocations is still embedding when the *other* one's Step 4 finishes and releases the session is the one that loses chunks — direction isn't fixed, but the mechanism and the fix are unchanged from 08-22.
     Magnitude: 184/1208 chunks failed (15.2%, vs 08-22's 21.3%) — by file: `state.md` (78), `lessons.md` (69), `backlog.md` (11), `backlog-archive.md` (9), `entities/projects/lodestar.md` (8), `eth-rnd-archive-notes/2026-08-29.md` (7), `entities/prs/pr-9914.md` (1), `2026-08-29.md` (1) — concentrated in the largest/most-chunked bank files, not this cron's own audit note. Verified the gap is scoped to the vector layer only: `qmd get memory-bank/state.md` returns current, correct content (document/FTS layer, Step 3, intact); `qmd status` shows all collections indexed and "updated 6h ago" (875 files/120055 vectors). Expected to self-heal on the next clean night the same way every prior vector gap has (embed only reprocesses content hashes still missing vectors). Also confirmed the 08-21 `MENTION_RE` fix is still holding: grepped tonight's log for `handelize`/`has no valid filename` — zero hits.
     Fleet-wide sanity check: only `weekly-stale-review-findings` remains at `consecutiveErrors >= 3` (unchanged at 4, next scheduled retry 2026-08-31T10:00 UTC per its Monday cadence — one day away now, unchanged from every check since 08-25, not a new problem). Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` byte-identical to the last six checks (2026-08-25T12:02:18Z) — zero new activity for the sixth straight day, still no reply to the 08-08 sign-off request, now **22 days pending**. This is fresh supporting evidence for the same already-pending ask (raising `timeoutSeconds` would give the first run room to finish before any retry fires, eliminating the overlap window — and this whole failure class — entirely), not a new decision, so no re-DM (calibrated-restraint precedent, same handling as 08-22's first data-loss instance). Posted routine status to topic #347. No config or memory-file edits; read-only investigation (this note lives under `notes/`, not `memory/`).
   - **2026-08-31 09:3x UTC re-check (`next-audit-priorities-reminder`):** live `cron list --json` confirms the config gap is still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, `nightly-memory-consolidation` all still have no `fallbacks` field on their payload; `cron-health-watchdog` still `delivery.mode: none`; `nightly-memory-consolidation` still `payload.timeoutSeconds: 900`. State: all four `consecutiveErrors: 0`, last runs ok. Tonight's `nightly-memory-consolidation` run (03:30:06-03:43:32 UTC, `lastDurationMs: 843078`≈14m03s) is a genuine clean single run, not retry-after-completion masking — finished well under the 900s timeout with no overlap, cross-checked against `memory/memory-cycle-2026-08-31.log` (476 chunks/8 documents, 13m6s CPU-bound QMD embedding on the still-unavailable nvcc/CUDA fallback — a light night, fourth occurrence of this lighter-corpus-clean-run pattern after 08-16, 08-23, 08-29). `bank/state.json` (3.68MB) and `.memory/index.sqlite` (7.52MB) both fresh at 03:30 UTC. Grepped tonight's log for `handelize`/`has no valid filename`/`SessionReleasedError`/`RangeError`/`overlapped` — zero hits (confirmed via explicit `$?`), confirming the 08-21 `MENTION_RE` fix is still holding with no regression. Fleet-wide sanity check (all 22 jobs): only 1 job at `consecutiveErrors >= 3` — `weekly-stale-review-findings` (unchanged at 4, still the 08-24 outage artifact, next scheduled retry today 2026-08-31T10:00 UTC per its Monday cadence — the wait flagged since 08-25 clears within the hour of this check, not a new problem). Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` byte-identical to the last seven checks (2026-08-25T12:02:18Z) — zero new activity for the seventh straight day, still no reply to the 08-08 sign-off request, now **23 days pending**. No new decision-relevant fact for the config ask itself, so no re-DM (calibrated-restraint precedent) — the config ask is unchanged and still gated on Nico's sign-off.
     **Separate blocker actually advanced this cycle (unilateral, non-config — same category as the 08-21 `MENTION_RE` fix): root-caused and mitigated the recurring overlap-induced embedding data loss (08-22, 08-30 — 2 confirmed instances, 269 and 184 chunks respectively) at the script level, without waiting on the cron-config sign-off.** Root cause, confirmed directly from this saga's own evidence: the cron harness's 900s `timeoutSeconds` monitoring window expires before the CPU-bound QMD embedding step finishes most nights, but — as repeatedly reconfirmed in the 22 prior daily entries above — the underlying `bash nightly_memory_cycle.sh` process is never killed when the harness gives up; it keeps running detached to real completion. When the harness's own automatic retry then starts a **second** full invocation of the identical script while the first is still mid-Step-4, both processes call `qmd embed` and race for the same shared embedding-model session; whichever one is still embedding when the other's Step 4 finishes and releases that session gets `SessionReleasedError` on its remaining chunks. **Fix applied to `scripts/memory/nightly_memory_cycle.sh`:** added an `flock` guard (`/tmp/lodekeeper-nightly-memory-cycle.lock`, held for the full script body via `exec 200>...` before Step 1) so a second invocation that starts while a prior one is still running now blocks until the first releases the lock, instead of racing it — serializes instead of corrupting, and preserves the retry's original safety-net purpose (it still runs, just after the first finishes rather than concurrently with it). No new unbounded hang risk: the cron harness's own per-invocation timeout still bounds a lock-waiting retry exactly as it already bounds every other step. **Verified before applying to the live pipeline:** `bash -n` clean; the exact `exec 200>...`/`flock -n`/`flock` idiom was extracted into an isolated throwaway test script and run as two real overlapping background processes (0.5s stagger) against a scratch lock file — confirmed the second process correctly detected contention, logged a "waiting" line, blocked for the remainder of the first's runtime, then proceeded only after the first released the lock (no concurrent execution window) — before touching the real script, and the real script's syntax was re-checked after editing. Also added a short **Concurrency** note to `docs/memory-system.md` documenting the new lock behavior and its cause. Not yet exercised against a real overlapping cron run (next nightly-memory-consolidation fires 2026-09-01 03:30 UTC) — flagging for tomorrow's re-check to confirm no regression (e.g. a legitimate hang if the lock is somehow never released) once it has run under real conditions. **Scope discipline:** this fixes the *symptom* (concurrent corruption) unilaterally; it does not fix the *cause* (900s timeout being too short for CPU-bound embedding) or reduce the number of harness-level "error" status mislabels — that half of the ask is still the same cron-config change pending Nico's sign-off, now **23 days pending**, unchanged by this fix. No `memory/*.md` files edited (hard constraint honored); no cron config edited. Posted routine status to topic #347 (genuine new fix this cycle, not a repeat of "still waiting").
   - **2026-09-01 09:3x UTC re-check (`next-audit-priorities-reminder`) — flock fix validated against a real overlapping run, exactly as flagged yesterday:** live `cron list --json` confirms the config gap is still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, `nightly-memory-consolidation` all still have no `fallbacks` field on their payload; `cron-health-watchdog` still `delivery.mode: none`; `nightly-memory-consolidation` still `payload.timeoutSeconds: 900`. State: all four `consecutiveErrors: 0`, last runs ok; fleet-wide sweep (22 jobs, all enabled, no duplicate names — the harmless duplicate `review-royale-weekly-digest` def noted in an unrelated 08-31 incident is gone) shows **zero** jobs at `consecutiveErrors >= 3` (the previously-flagged `weekly-stale-review-findings` cleared on schedule 2026-08-31, as predicted 08-25 through 08-31).
     **Yesterday's `flock` guard (added to `scripts/memory/nightly_memory_cycle.sh` to stop the overlap-induced embedding data loss seen 08-22/08-30) got its first live test tonight and passed cleanly.** `memory/memory-cycle-2026-09-01.log`: the regular 03:30:41 UTC run took the lock and ran a full 28m40s (1300 chunks/10 documents CPU-bound embed on the still-unavailable nvcc/CUDA fallback — unrelated to the fix), which as always exceeded the 900s cron-harness timeout and triggered an automatic retry. Instead of racing for the shared embedding session like 08-22/08-30, the retry correctly **blocked on the lock** (`"Lock acquired after wait — prior cycle finished, proceeding"` logged at 03:59:30Z, the exact moment the first run's own completion line appears) and only proceeded once the first released it — found nothing new to do (`Added: 0`, `✓ All content hashes already have embeddings`) and exited 2s later. Live cron state confirms this landed as a clean `ok` (`lastDurationMs: 751016`≈12m31s, consistent with ~12.5min blocked on the lock + 2s of real work, not a race), `consecutiveErrors: 0`. Grepped the full log for every previously-seen failure signature — `SessionReleasedError` (0 hits), `handelize`/`has no valid filename`, `RangeError`, `overlapped`, `chunks failed` — all clean. `bank/state.json` (3.68MB) and `.memory/index.sqlite` (7.52MB) both fresh at 03:59 UTC; `qmd status` confirms all 3 collections indexed (`daily-notes` 488, `memory-bank` 305, `workspace-core` 86 files, all "updated 6h ago"). No hang materialized (the one real theoretical risk flagged yesterday) — the retry's own harness timeout window comfortably covered its ~12.5min lock wait.
     **Net effect on the standing ask:** this closes out the *data-loss* half of the `nightly-memory-consolidation` timeout problem unilaterally (no more overlap corruption, now confirmed under real conditions) — it does not fix the *cosmetic* half (harness still marks a successfully-completing run "error" and burns a ~12-15min blocked retry every night the primary run exceeds 900s), which still needs Nico's sign-off to raise `timeoutSeconds` to ~2700s, same as the still-open fallback-config ask for the other 3 jobs + `cron-health-watchdog` delivery routing.
     Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` advanced to 2026-08-31T20:51:27Z (new activity since the last check), but it's a **separate, already-closed** incident — 5 hung isolated cron sessions (`monitor-open-pr-ci`, `workspace-periodic-cleanup`, `cron-health-watchdog`, `Devnet health monitor`, `Sync dotfiles repo`) pegged the gateway event loop for 100+min each; Nico restarted the gateway, that session verified the recovery and wrote up a new memory reference on the pattern, and left a standing offer to investigate why `timeoutSeconds` isn't hard-killing hung runs (unanswered, not re-nudging — his call, separate from this cron's own tracked ask). Grepped the full transcript for fallback/sign-off/consolidation/timeoutSeconds/quota/2700/nightly-memory keywords beyond the incidental "cron-health-watchdog" hit (one of the 5 stuck jobs in that unrelated incident) — nothing addresses the standing sign-off ask. Still no reply to the 08-08 request, now **24 days pending**. No new decision-relevant fact for the config ask itself (if anything, today's fix *reduces* its urgency by removing the data-loss consequence), so no re-DM (calibrated-restraint precedent). Posted routine status + the fix validation to topic #347. No config or memory-file edits; only `notes/autonomy-gaps.md` (this file) updated.
   - **2026-09-02 09:3x UTC re-check (`next-audit-priorities-reminder`) — flock fix 2-for-2, DM thread unchanged:** live `cron list --json` confirms the config gap is still present and unchanged for the original four jobs: `self-improvement-audit-daily`, `cron-health-watchdog`, `check-openclaw-release`, `nightly-memory-consolidation` all still have no `fallbacks` field on their payload; `cron-health-watchdog` still `delivery.mode: none`; `nightly-memory-consolidation` still `payload.timeoutSeconds: 900`. State: all four `consecutiveErrors: 0`, last runs ok; fleet-wide sweep (22 jobs) shows zero jobs at `consecutiveErrors >= 3`.
     **Second consecutive clean night for the `flock` guard** (first live-tested 09-01): `cron runs --id 4aaaf7f7-...` shows the regular 03:30:00.010 UTC run hit the same already-diagnosed 900s harness timeout (`status: error`, `errorReason: timeout`, `durationMs: 900567`, harness gave up 03:45:00.679 UTC), and the automatic retry starting 03:46:46.220 UTC correctly blocked on the lock rather than racing for the shared embedding session — cross-checked against `memory/memory-cycle-2026-09-02.log`: `[03:47:05Z] Another nightly memory cycle is already running — waiting for it to finish` / `[03:57:55Z] Lock acquired after wait — prior cycle finished, proceeding`, then found nothing new to do and exited cleanly (retry's own summary: "The run waited for an overlapping cycle to release the lock, then proceeded successfully... The prior overlapping run embedded 1,248 chunks from 10 documents"). Grepped for every previously-seen failure signature — `SessionReleasedError`, `handelize`/`has no valid filename`, `RangeError`, `overlapped`, `chunks failed` — zero hits. `bank/state.json` (3.69MB) and `.memory/index.sqlite` (7.52MB) both fresh at 03:57 UTC. Two-for-two on real overlapping nights since the fix landed 08-31; the data-loss failure mode (08-22, 08-30) looks durably closed.
     Checked the Nico DM thread directly (session `agent:main:telegram:direct:5774760693`, id `1426909a-...`): `updatedAt` byte-identical to yesterday's check (2026-08-31T20:51:27Z) — zero new activity since the hung-cron-sessions gateway-restart incident already logged in yesterday's entry, still no reply to the 08-08 sign-off request, now **25 days pending**. No new decision-relevant fact, so no re-DM (calibrated-restraint precedent, unchanged for 3+ weeks). **Standing ask is now purely cosmetic + safety-net, not data-integrity:** the only remaining gap is (a) the harness mislabeling successfully-completing-but-slow runs as `error` (fixable by raising `timeoutSeconds` to ~2700s) and (b) no cross-provider fallback on the 4 Codex-subscription-backed jobs for a future quota-exhaustion window (same class as the already-self-healed 08-06→08-08 incident that started this thread) — both still cron-config edits gated on Nico's sign-off, not something to patch unilaterally. Posted routine status to topic #347. No config or memory-file edits; only `notes/autonomy-gaps.md` (this file) updated.
